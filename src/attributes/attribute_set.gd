## Just acts as a container for attributes. I am just marking this as abstract to enforce that we explicitly
## extend sets of attributes to ensure type safety
class_name AttributeSet extends Resource
